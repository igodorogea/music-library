'use strict';

angular.module('musicLibrary',
  [
    'ngAnimate',
    'ngMessages',
    'ngAria',
    'ngResource',
    'ui.router',
    'ngMaterial'
  ])
  // .config(config)
  // .config(routerConfig)
  // .run(runBlock);